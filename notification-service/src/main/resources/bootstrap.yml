spring:
  application:
    name: notification-service
  cloud:
    consul:
      host: localhost
      port: 8500
    config:
      discovery:
        enabled: true
        service-id: config
      fast-fast: true
      password: ${CONFIG_SERVICE_PASSWORD}
      username: user