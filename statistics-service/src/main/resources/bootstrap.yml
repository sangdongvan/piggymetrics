spring:
  application:
    name: statistics-service
  cloud:
    config:
      discovery:        enabled: true
        service-id: config
      fail-fast: true
      password: ${CONFIG_SERVICE_PASSWORD}
      username: user