spring:
  application:
    name: account-service
  cloud:
    config:
      discovery:
        enabled: true
        service-id: config
      failFast: true
      username: user
      password: ${CONFIG_SERVICE_PASSWORD}
